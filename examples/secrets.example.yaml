# Copy this file to secrets.yaml and fill in your own values.
# Never commit secrets.yaml.

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"

# Generate/use your own ESPHome API encryption key.
api_encryption_key: "YOUR_ESPHOME_API_ENCRYPTION_KEY"
